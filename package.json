{
  "name": "gameverse-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd gameverse_frontend && npm install && npm run build",
    "start": "node gameverse_backend/index.js"
  }
}
